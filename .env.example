# OpenAI (required for AI summary + quiz generation)
# 1. Go to https://platform.openai.com/api-keys
# 2. Sign in or create an account, add billing if prompted
# 3. Click "Create new secret key", copy it and set below (never commit .env.local)
OPENAI_API_KEY=sk-your-key-here

# Supabase (recommended for production persistence)
# 1. Create project at https://supabase.com/dashboard
# 2. In Project Settings -> API, copy URL and service role key
# 3. Run supabase/schema.sql in SQL Editor
# 4. Add values below (keep service role key server-side only)
#    - URL + service_role key are in Supabase Dashboard -> Project Settings -> API
#    - Storage uses bucket name only; there is no separate "bucket key"
# SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SUPABASE_STORAGE_BUCKET=handouts
